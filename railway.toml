[build]
builder = "nixpacks"
buildCommand = "rm -rf dist node_modules && npm install && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
